The Subro.Json library is a simple place to put JSON related code. Its main purpose is to have a single point of truth
for Json type information.

The generator is for future development. Hopefully the generator chain will be resolved OR json type info can be generated in another way

